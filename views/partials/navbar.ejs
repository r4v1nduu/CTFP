<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand mx-4" href="/highscore">CTFP</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto">
            <!-- Highscore link -->
            <li class="nav-item">
                <a class="nav-link btn btn-primary text-white" href="/highscore">Highscore</a>
            </li>
            <% if (user && user.role === 'superadmin') { %>
                <!-- Manage Players link for superadmin -->
                <li class="nav-item">
                    <a class="nav-link btn btn-primary text-white" href="/manage-players">Manage Players</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary text-white" href="/manage-flags">Manage Flags</a>
                </li>
            <% } %>
            <!-- Add more links as needed -->
        </ul>
        <ul class="navbar-nav ml-auto">
            <% if (!user) { %>
                <!-- Display login link -->
                <li class="nav-item">
                    <a class="nav-link btn btn-primary text-white" href="/login">Login</a>
                </li>
            <% } else { %>
                <!-- Display Hi! {username} and Logout links -->
                <li class="nav-item">
                    <span class="nav-link text-white">Hi! <%= user.username %></span>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary text-white" href="/logout">Logout</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>
